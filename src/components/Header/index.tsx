import { Container, LogoImage } from './styles';

const Header = () => {
  return (
    <Container>
      <LogoImage src="/logo.png" alt="Buildbox - WEB CHALLENGE" />
    </Container>
  );
};

export default Header;
